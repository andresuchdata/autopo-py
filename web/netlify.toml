[build]
  command = "bun run build"
  publish = "out"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  framework = "next"
  targetPort = 3000

# Required for Next.js API routes to work
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
